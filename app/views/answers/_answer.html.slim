p= answer.body
- if current_user && current_user.id == answer.user_id
    p= link_to 'Delete answer', question_answer_path(@question, answer), method: :delete, class: 'delete_answer btn btn-sm btn-warning'